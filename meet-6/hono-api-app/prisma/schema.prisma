generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model Transactions {
  id Int @id @default(autoincrement())
  description String
  price Int
}